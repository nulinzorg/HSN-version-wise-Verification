flask>=3.0.0
requests>=2.31.0
pandas>=2.0
openpyxl>=3.1
lxml>=5.0

# Email alerts (notifier.py) use only Python's built-in smtplib/email
# modules — no extra package needed, no cost beyond your own email account.
